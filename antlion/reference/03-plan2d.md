---
layout: default
title: "Section 3 · 2D Plan Layout"
generated: true
---

# Section 3 · 2D Plan Layout

`Antlion ▸ Bowl ▸ Section 3` — unroll the section onto the plan, place vomitories and seats, then cut the bowl to shape. **Most design decisions happen here.**

> Generated from the plugin source by `refdoc_check.py --sync` — the text below is
> the plugin's own tooltip text. Do not edit this file; it is rewritten on every sync.
> Layout, standard chains and shared ports: [README](index.md).

---

## Stand 2D

`Antlion ▸ Bowl ▸ Section 3` · ID `STAND2D-002` · 2 inputs · 5 outputs

> Sweeps each tier section around the axis planes and flattens it into tread rings in plan. Everything downstream — seats, vomitories, cuts — works on these rings.

**Inputs**

0. `Stand Sections`  ·  nick `SSD` — `Object` `list` · optional
   - The sections to lay out, from Section or Section Plus. One branch per tier

1. `Axis` — `Object` `item` · optional
   - The axis system - supplies the placement planes and whether the ring closes

**Outputs**

0. `Stand Sections`  ·  nick `SSD` — `Object` `list`
   - Pass-through of the sections, unchanged. Wire straight on to Vomitory

1. `Axis` — `Object` `item`
   - Pass-through of the axis, unchanged. Wire straight on to Vomitory

2. `Stand 2D` — `Object` `item`
   - The 2D bowl as one object - every tier's tread rings in plan

3. `Tread Rings`  ·  nick `Rings` — `Curve` `list`
   - The tread rings themselves, all tiers in one flat list. For preview

4. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Vomitory

`Antlion ▸ Bowl ▸ Section 3` · ID `VOMITORY-001` · 18 inputs · 8 outputs

> Places the vomitories — the tunnels spectators enter the bowl through — in the tread rings, and sets how each one gets down to the concourse: flat, stepped, stair or ramp.

**Inputs**

0. `Stand Sections`  ·  nick `SSD` — `Object` `list` · optional
   - The tier sections, passed along the chain. Wire from Stand 2D SSD

1. `Axis` — `Object` `item` · optional
   - The axis system, passed along the chain. Wire from Stand 2D

2. `Stand 2D` — `Object` `item` · optional
   - The 2D bowl the vomitories are cut into. Wire from Stand 2D

3. `Vom Strides`  ·  nick `Stride` — `Integer` `list` · optional
   - Place a vomitory every N ring segments - one per tier (default 2)

4. `Vom Start Index`  ·  nick `Start Ix` — `Integer` `item` · default `0`
   - Which ring segment the first vomitory lands on (default 0)

5. `Vom Start Treads`  ·  nick `Start Tr` — `Integer` `list` · optional
   - Which tread the opening starts at, front row = 1 - one per tier (default 1)

6. `Vom Type Mode`  ·  nick `▾ Type` — `Integer` `list` · optional · dropdown
   - How each tier reaches the concourse: 1 BASIC, 2 STEPPED, 3 STAIR, 4 RAMP (default 1)

7. `Vom Widths`  ·  nick `Vom W` — `Number` `list` · optional
   - Clear width of the vomitory opening - one per tier (default 3000 mm)

8. `Min Clearance`  ·  nick `Min Clr` — `Number` `item` · default `2400 mm / 96 in`
   - Headroom kept over the flat parts. Openings that fall short are warned (default 2400 mm)

9. `Stair Clearance`  ·  nick `Stair Clr` — `Number` `item` · default `2100 mm / 84 in`
   - Headroom kept over the sloping part of a stair or ramp (default 2100 mm)

10. `Vom Drops`  ·  nick `Drop` — `Number` `list` · optional
   - How far the vomitory drops to reach the concourse - one per tier. Types 3 and 4 only (default 0)

11. `Landing Depths`  ·  nick `Land D` — `Number` `list` · optional
   - Depth of the flat waiting space at the top - one per tier. 0 = same as the tread it replaces (default 0)

12. `Ramp Slope`  ·  nick `Slope` — `Number` `item` · default `12.0`
   - Ramp gradient as 1:N - 12 means 1 in 12. Type 4 only (default 12)

13. `Mid Landing Depth`  ·  nick `Mid D` — `Number` `item` · default `0.0`
   - Depth of each intermediate landing. 0 = code minimum (default 0)

14. `Vom Enabled`  ·  nick `Vom On` — `Boolean` `list` · optional
   - Which tiers get vomitories - one per tier, empty = all on (default true)

15. `Align Tiers`  ·  nick `Align` — `Boolean` `item` · default `true`
   - On = vomitory axes derive from tier 0 and line up across tiers (default true)

16. `Mid Landings`  ·  nick `Mid Land` — `Boolean` `list` · optional
   - Break long descents with intermediate landings - one per tier (default true)

17. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Vomitory`  ·  nick `VD` — `Object` `item`
   - The vomitories as one object - footprints, tread ranges, descent data. Wire into Seat

1. `Stand 2D` — `Object` `item`
   - Pass-through of the 2D bowl, unchanged. Wire straight on to Seat

2. `Stand Sections`  ·  nick `SSD` — `Object` `list`
   - Pass-through of the sections, unchanged. Wire straight on to Seat

3. `Axis` — `Object` `item`
   - Pass-through of the axis, unchanged. It carries the axis numbers to Cut

4. `Vom Lines`  ·  nick `Vom Lns` — `Curve` `tree`
   - One branch per tier. The vomitory footprints in plan, empty branch when none

5. `Section Lines`  ·  nick `Sec Lns` — `Curve` `list`
   - The tier section lines the descent was worked out against

6. `Vom Section Rects`  ·  nick `Vom Sec` — `Curve` `list`
   - The vomitory opening seen in section - what gets cut out of the stand

7. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Seat

`Antlion ▸ Bowl ▸ Section 3` · ID `SEAT-001` · 19 inputs · 10 outputs

> Fills the tread rings with seats, breaking each row at the vomitories and aisles, and flags rows that break the egress limits. Wheelchair spaces are placed here too.

**Inputs**

0. `Vomitory`  ·  nick `VD` — `Object` `item` · optional
   - The vomitories seats must avoid. Wire from Vomitory VD

1. `Stand 2D` — `Object` `item` · optional
   - The tread rings seats are placed on. Wire from Vomitory

2. `Stand Sections`  ·  nick `SSD` — `Object` `list` · optional
   - The tier sections, passed along the chain. Wire from Vomitory SSD

3. `Axis` — `Object` `item` · optional
   - The axis system, passed along. It carries the axis numbers Cut refers to

4. `Max Seats Per Run`  ·  nick `Max Run` — `Integer` `item` · default `28`
   - Longest run allowed when spectators can leave from both ends (default 28)

5. `Min Seats Per Run`  ·  nick `Min Run` — `Integer` `item` · default `4`
   - Shortest run worth keeping. Anything shorter is flagged POOR (default 4)

6. `Aisle Side Mode`  ·  nick `▾ Side` — `Integer` `list` · optional · dropdown
   - Which side of a tier gets a stepped aisle: BOTH, LEFT, RIGHT or NONE - one per tier. This is about walking up and down, not about the open ends - for those see End Aisle Mode (default BOTH)

7. `Vom Shift`  ·  nick `Shift` — `Integer` `item` · default `0`
   - 1 = let vomitory centrelines slide along straight runs to cut seat stubs (default 0)

8. `Seat Width`  ·  nick `Seat W` — `Number` `item` · default `500 mm / 20 in`
   - Width of one seat - this sets how many fit per row (default 500 mm)

9. `Seat Depth`  ·  nick `Seat D` — `Number` `item` · default `500 mm / 20 in`
   - Depth of one seat (default 500 mm)

10. `Aisle Width`  ·  nick `Aisle W` — `Number` `item` · default `1000 mm / 44 in`
   - Clear width of each aisle (default 1000 mm)

11. `Max Riser Step`  ·  nick `Max Step` — `Number` `item` · default `180 mm / 7 in`
   - Comfortable rise per aisle step. Taller risers get up to 2 steps (default 180 mm)

12. `WC Ratio Target`  ·  nick `WC %` — `Number` `item` · default `0.0`
   - Target share of wheelchair spaces, percent of capacity. Reporting only (default 0)

13. `WC Width`  ·  nick `WC W` — `Number` `item` · default `900 mm / 36 in`
   - Width of one wheelchair space. 900 mm follows FIFA and EN 13200-1, 36 in follows ADA (default 900 mm)

14. `WC Depth`  ·  nick `WC D` — `Number` `item` · default `1400 mm / 48 in`
   - Depth of one wheelchair space. 1400 mm follows FIFA and EN 13200-1, 48 in follows ADA (default 1400 mm)

15. `Aisle Split Mode`  ·  nick `Split` — `Boolean` `item` · default `true`
   - Add a mid aisle to any run still too long. Off leaves them alone and flagged (default true)

16. `Aisle Full Height`  ·  nick `Full H` — `Boolean` `item` · default `true`
   - On = the aisle runs the full tier height, off = only from where the row gets too long

17. `End Aisle Mode`  ·  nick `End A` — `Boolean` `item` · default `false`
   - On = leave a walking strip along each open end of a run instead of seats, so spectators can get out sideways. Off = seat right up to the end (default false)

18. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Seat Result`  ·  nick `SRD` — `Object` `item`
   - The whole 2D layout as one object. Wire into Cut, Stand 3D Plus or Vom 3D

1. `Seat Rects`  ·  nick `Seats` — `Curve` `tree`
   - One branch per tier. Every seat as a rectangle in plan

2. `Good Seat Rects`  ·  nick `Good` — `Curve` `tree`
   - One branch per tier. Seats in runs that meet the egress rules

3. `Poor Seat Rects`  ·  nick `Poor` — `Curve` `tree`
   - One branch per tier. Seats in runs that break the rules - these are what to fix

4. `Vom Rects`  ·  nick `Voms` — `Curve` `tree`
   - One branch per tier. The vomitory footprints as actually used here

5. `Vom Shifts`  ·  nick `Shifts` — `Number` `tree`
   - One branch per tier. How far each vomitory slid, 0 = not moved

6. `Aisles` — `Curve` `tree`
   - One branch per tier. The aisle floor as cells, tread by tread - intermediate steps where the riser needs them (up to 2), one full-tread cell where it does not

7. `WC Platforms`  ·  nick `WC Plat` — `Curve` `tree`
   - One branch per tier. The level platform an accessible row sits on

8. `WC Units` — `Curve` `tree`
   - One branch per tier. Two rects per unit - wheelchair space, then companion seat

9. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Custom Seat

`Antlion ▸ Bowl ▸ Section 3` · ID `SEAT-002` · 19 inputs · 9 outputs

> Same seat placement as Seat, but the vomitories, aisles and even the seats come from curves you draw instead of being generated. Use it where the automatic layout cannot express what you want.

**Inputs**

0. `Stand Sections`  ·  nick `SSD` — `Object` `list`
   - The tier sections, passed along the chain. Wire from Stand 2D SSD

1. `Axis` — `Object` `item` · optional
   - The axis system from Stand 2D. Without it, curves are found from turn angles

2. `Stand 2D` — `Object` `item`
   - The tread rings seats are placed on. Wire from Stand 2D

3. `Custom Voms`  ·  nick `Cust Vom` — `Curve` `tree` · optional
   - One branch per tier. Draw each rect over the treads the opening spans

4. `Custom Steps`  ·  nick `Cust Step` — `Curve` `tree` · optional
   - One branch per tier. Feed baked Aisles cells back, or draw your own - open curve = the aisle centreline, closed rect = one cell placing the aisle and its tread range

5. `Custom Seats`  ·  nick `Cust Seat` — `Curve` `tree` · optional
   - One branch per tier. A tier given seats here is only judged, not laid out

6. `Custom WCs`  ·  nick `Cust WC` — `Curve` `tree` · optional
   - One branch per tier. Draw each accessible space - wheelchair and companion alike - exactly where you want it. The rects are kept as drawn, seats underneath are removed, and automatic WC units are replaced by yours - the accessible row itself stays cleared and merged

7. `Max Seats Per Run`  ·  nick `Max Run` — `Integer` `item` · default `28`
   - Longest run allowed when spectators can leave from both ends (default 28)

8. `Min Seats Per Run`  ·  nick `Min Run` — `Integer` `item` · default `4`
   - Shortest run worth keeping. Anything shorter is flagged POOR (default 4)

9. `Aisle Side Mode`  ·  nick `▾ Side` — `Integer` `list` · optional · dropdown
   - Where the end aisles go: BOTH, LEFT, RIGHT or NONE - one per tier, the last value fills the rest (default BOTH)

10. `Seat Width`  ·  nick `Seat W` — `Number` `item` · default `500 mm / 20 in`
   - Width of one seat (default 500 mm)

11. `Seat Depth`  ·  nick `Seat D` — `Number` `item` · default `500 mm / 20 in`
   - Depth of one seat (default 500 mm)

12. `Aisle Width`  ·  nick `Aisle W` — `Number` `item` · default `1000 mm / 44 in`
   - Clear width of each aisle (default 1000 mm)

13. `Max Riser Step`  ·  nick `Max Step` — `Number` `item` · default `180 mm / 7 in`
   - Comfortable rise per aisle step. Taller risers get up to 2 steps (default 180 mm)

14. `Aisle Split Mode`  ·  nick `Split` — `Boolean` `item` · default `true`
   - Add a mid aisle to runs still too long. Your own aisles never move (default true)

15. `WC Ratio Target`  ·  nick `WC %` — `Number` `item` · default `0.0`
   - Target share of wheelchair spaces, percent of capacity. Reporting only (default 0)

16. `WC Width`  ·  nick `WC W` — `Number` `item` · default `900 mm / 36 in`
   - Width of one wheelchair space. 900 mm follows FIFA and EN 13200-1, 36 in follows ADA (default 900 mm)

17. `WC Depth`  ·  nick `WC D` — `Number` `item` · default `1400 mm / 48 in`
   - Depth of one wheelchair space. 1400 mm follows FIFA and EN 13200-1, 48 in follows ADA (default 1400 mm)

18. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Seat Result`  ·  nick `SRD` — `Object` `item`
   - The whole 2D layout as one object. Wire into Cut, Stand 3D Plus or Vom 3D

1. `Seat Rects`  ·  nick `Seats` — `Curve` `tree`
   - One branch per tier. Every seat as a rectangle in plan

2. `Good Seat Rects`  ·  nick `Good` — `Curve` `tree`
   - One branch per tier. Seats in runs that meet the egress rules

3. `Poor Seat Rects`  ·  nick `Poor` — `Curve` `tree`
   - One branch per tier. Seats in runs that break the rules

4. `Vom Rects`  ·  nick `Voms` — `Curve` `tree`
   - One branch per tier. Your vomitory footprints as actually applied

5. `Aisles` — `Curve` `tree`
   - One branch per tier. The aisle floor as cells, tread by tread - intermediate steps where the riser needs them (up to 2), one full-tread cell where it does not

6. `WC Platforms`  ·  nick `WC Plat` — `Curve` `tree`
   - One branch per tier. The level platform an accessible row sits on

7. `WC Units` — `Curve` `tree`
   - One branch per tier. Two rects per unit - wheelchair space, then companion seat

8. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Cut

`Antlion ▸ Bowl ▸ Section 3` · ID `CUT-001` · 10 inputs · 9 outputs

> Cuts the bowl to shape — keep or remove a whole span between two axis numbers, or everything on one side of a curve you draw. Seats, aisles, vomitories and rings are all filtered together.

**Inputs**

0. `Seat Result`  ·  nick `SRD` — `Object` `item` · optional
   - The layout to cut. Wire from Seat SRD, or from another Cut to stack cuts

1. `Cut Curve`  ·  nick `Curve` — `Curve` `item` · optional
   - Cut along this curve, drawn in plan. Past its endpoints nothing changes

2. `Axis From`  ·  nick `From` — `Integer` `list` · optional
   - Start axis number of each span to cut. Read the numbers off View Axis. -1 = skip

3. `Axis To`  ·  nick `To` — `Integer` `list` · optional
   - End axis number of each span, matching Axis From item for item (no default)

4. `Cut Combine Mode`  ·  nick `▾ Comb` — `Text` `item` · default `AND` · dropdown
   - With both a curve and a span set: AND applies both, or use only one (default AND)

5. `Keep Side`  ·  nick `Keep Sd` — `Boolean` `item` · default `true`
   - Which side of your curve survives, looking along it: on = left (default true)

6. `End Aisle` — `Boolean` `item` · default `false`
   - Curve mode only. Pull seats back one aisle width so the new edge can escape (default false)

7. `Keep Span`  ·  nick `Keep Sp` — `Boolean` `item` · default `true`
   - On = keep only what is inside the spans, off = remove what is inside (default true)

8. `Tier Enabled`  ·  nick `Tiers` — `Boolean` `list` · optional
   - Which tiers this cut applies to. Tiers switched off pass through untouched (default true)

9. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Seat Result`  ·  nick `SRD` — `Object` `item`
   - The layout after the cut. Chain straight into another Cut or Stand 3D Plus

1. `Cut Lines`  ·  nick `Cut Lns` — `Curve` `list`
   - Where the cut actually landed. Check this first when a cut looks wrong

2. `Cut Records`  ·  nick `Records` — `Text` `list`
   - A line per cut made here - tier, axis range, row range and kind

3. `Removed Seat Rects`  ·  nick `Removed` — `Curve` `list`
   - The seats this cut removed, drawn in plan

4. `Tread Rings`  ·  nick `Rings` — `Curve` `tree`
   - One branch per tier. The tread rings after the cut, whole bowl included

5. `Seat Rects`  ·  nick `Seats` — `Curve` `tree`
   - One branch per tier. The seats that survived

6. `Vom Rects`  ·  nick `Voms` — `Curve` `tree`
   - One branch per tier. The vomitories that survived

7. `Aisles` — `Curve` `tree`
   - One branch per tier. The aisle cells that survived

8. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Opening Cut

`Antlion ▸ Bowl ▸ Section 3` · ID `CUT-002` · 11 inputs · 11 outputs

> Punches local entrance openings through the stand — between two axis numbers, or under a closed curve you draw. Unlike Cut it removes only what the opening covers, and it records the new edges as railing lines.

**Inputs**

0. `Seat Result`  ·  nick `SRD` — `Object` `item` · optional
   - The layout to open up. Wire from Seat SRD, or from a Cut to stack them

1. `Cut Curve`  ·  nick `Curve` — `Curve` `item` · optional
   - A CLOSED curve in plan - everything inside it is opened up. Open curves are ignored

2. `Opening From`  ·  nick `From` — `Integer` `list` · optional
   - One axis number per opening. Order does not matter - the span runs the short way, never wrapping past axis 0 (no default)

3. `Opening To`  ·  nick `To` — `Integer` `list` · optional
   - The other axis number of each opening, matching Opening From item for item (no default)

4. `Rows` — `Integer` `list` · optional
   - How many treads each opening removes, counting up from the bottom of the tier - one per opening, so opening 1 can cut 3 rows while opening 2 cuts 10 (default 0)

5. `Opening Type Mode`  ·  nick `▾ Type` — `Text` `list` · optional · dropdown
   - SPAN empties the whole width, VOM cuts a rectangle at the midpoint - one per opening (default SPAN)

6. `Cut Combine Mode`  ·  nick `▾ Comb` — `Text` `item` · default `BOTH` · dropdown
   - With both openings and a curve set: BOTH cuts either, or use only one (default BOTH)

7. `Opening Widths`  ·  nick `Open W` — `Number` `list` · optional
   - Width of a VOM opening - one per opening, ignored by SPAN (default 1200 mm)

8. `Min Tread Depth`  ·  nick `Min Trd` — `Number` `item` · default `0.0`
   - Where a curve leaves a tread shallower than this, its seats go too. 0 = off (default 0)

9. `Tier Enabled`  ·  nick `Tiers` — `Boolean` `list` · optional
   - Which tiers these openings cut through. Empty = every tier (default true)

10. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Seat Result`  ·  nick `SRD` — `Object` `item`
   - The layout after the openings. Chain straight into another cut or Stand 3D Plus

1. `Railing Lines`  ·  nick `Rail Lns` — `Curve` `list`
   - Every edge that now needs a railing, running total. Rail 3D builds from these

2. `Opening Rects`  ·  nick `Openings` — `Curve` `list`
   - The outline of each opening this component cut

3. `Cut Records`  ·  nick `Records` — `Text` `list`
   - A line per opening made here - tier, axis range, row range

4. `Removed Seat Rects`  ·  nick `Removed` — `Curve` `list`
   - The seats these openings removed, drawn in plan

5. `Tread Rings`  ·  nick `Rings` — `Curve` `tree`
   - One branch per tier. The tread rings after the openings

6. `Seat Rects`  ·  nick `Seats` — `Curve` `tree`
   - One branch per tier. The seats that survived

7. `Vom Rects`  ·  nick `Voms` — `Curve` `tree`
   - One branch per tier. The vomitories that survived

8. `Aisles` — `Curve` `tree`
   - One branch per tier. The aisle cells that survived

9. `WC Units` — `Curve` `tree`
   - One branch per tier. A unit loses its floor if an opening took the treads under it

10. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

◀ [Section 2 · Section](02-section.md) · next ▶ [Section 4 · 3D](04-body3d.md)
