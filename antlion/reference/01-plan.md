---
layout: default
title: "Section 1 · Plan & Setup"
generated: true
---

# Section 1 · Plan & Setup

`Antlion ▸ Bowl ▸ Section 1` — place the field, draw the line where the front row starts, and set the axes the sections will sit on. Everything starts here.

> Generated from the plugin source by `refdoc_check.py --sync` — the text below is
> the plugin's own tooltip text. Do not edit this file; it is rewritten on every sync.
> Layout, standard chains and shared ports: [README](index.md).

---

## Field

`Antlion ▸ Bowl ▸ Section 1` · ID `FIELD-001` · 9 inputs · 6 outputs

> Draws the playing field outline for a sport and the focal point every sightline is measured to. Start here — the rest of the chain hangs off field_info.

**Inputs**

0. `Custom Curve`  ·  nick `Curve` — `Curve` `item` · optional
   - Your own field outline, a closed curve in plan. Used only when Sport Mode is CUSTOM

1. `Base Plane`  ·  nick `Plane` — `Plane` `item` · default `Plane.WorldXY`
   - Where the field sits and which way it faces (default world XY)

2. `Custom Focal Point`  ·  nick `Custom F` — `Point` `item` · optional
   - Your own focal point. Used only when Focal Position Mode is CUSTOM

3. `Sport Mode`  ·  nick `▾ Sport` — `Text` `item` · default `SOCCER` · dropdown
   - Which sport sets the field size and shape. CUSTOM takes your own curve (default SOCCER)

4. `Focal Position Mode`  ·  nick `▾ Focal` — `Text` `item` · default `HALF_S` · dropdown
   - Where on the field the focal point sits (default HALF_S, touchline at halfway)

5. `Focal Direction Mode`  ·  nick `▾ Focal` — `Text` `item` · default `VERTICAL` · dropdown
   - VERTICAL lifts the focal point above the field, HORIZONTAL pulls it inside the outline (default VERTICAL)

6. `Focal Offset`  ·  nick `Focal Off` — `Number` `item` · default `1500 mm / 60 in`
   - How far the focal point sits from the field surface or outline - Focal Direction says which way. Ball on the ground is 0 (default 1500 mm)

7. `Rotate 90`  ·  nick `Rotate` — `Boolean` `item` · default `false`
   - Turn the field a quarter turn so its long side runs the other way (default false)

8. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Field Info`  ·  nick `Field` — `Object` `item`
   - The field as one object - outline, frame, sport and focal point

1. `Field Outline`  ·  nick `Outline` — `Curve` `item`
   - The playing field outline in plan

2. `Focal Curve`  ·  nick `Focal C` — `Curve` `item`
   - The outline moved by Focal Offset - lifted (VERTICAL) or pulled inside (HORIZONTAL)

3. `Focal Point`  ·  nick `Focal` — `Point` `item`
   - The point every sightline aims at

4. `Base Point`  ·  nick `Base Pt` — `Point` `item`
   - The same point on the field outline, before Focal Offset was applied

5. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Startline Rect

`Antlion ▸ Bowl ▸ Section 1` · ID `STARTLINE-001` · 7 inputs · 5 outputs

> Start line for the front row, offset outward from the field edge by a long-side and a short-side distance. Optional rounded or chamfered corners.

**Inputs**

0. `Field Info`  ·  nick `Field` — `Object` `item` · optional
   - The field, straight from Field - gives the outline and its rotation

1. `Source Outline`  ·  nick `Source` — `Curve` `item` · optional
   - Offset from this curve instead. Used only when Field Info is not wired

2. `Corner Mode`  ·  nick `▾ Corner` — `Text` `item` · default `StartlineConstants.DefaultCornerMode` · dropdown
   - How the four corners are treated: square, arc or straight cut (default NONE)

3. `Offset Long`  ·  nick `Off Long` — `Number` `item` · default `StartlineConstants.DefaultOffsetLong`
   - Gap from the long sides of the field to the front row (default 5000 mm)

4. `Offset Short`  ·  nick `Off Short` — `Number` `item` · default `StartlineConstants.DefaultOffsetShort`
   - Gap from the short (goal) ends of the field to the front row (default 5000 mm)

5. `Corner Size`  ·  nick `Corner S` — `Number` `item` · default `StartlineConstants.DefaultCornerSize`
   - Corner arc radius, or chamfer cut-back. Ignored when Corner Mode is NONE (default 2000 mm)

6. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Start Line`  ·  nick `Start` — `Object` `item`
   - The start line as one object - curve plus its arc data. Wire into Axis

1. `Start Line Curve`  ·  nick `Curve` — `Curve` `item`
   - The start line itself, a closed curve in plan

2. `Arc Centers`  ·  nick `Centers` — `Point` `list`
   - Centre point of each rounded corner (empty unless Corner Mode is ROUND)

3. `Arc Radii`  ·  nick `Radii` — `Number` `list`
   - Radius of each corner arc, matching Arc Centers

4. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Startline Capsule

`Antlion ▸ Bowl ▸ Section 1` · ID `STARTLINE-003` · 5 inputs · 5 outputs

> Start line as a capsule - two equal semicircles joined by straight sides. The classic athletics track outline.

**Inputs**

0. `Field Info`  ·  nick `Field` — `Object` `item` · optional
   - The field, straight from Field - gives the outline and its rotation

1. `Source Outline`  ·  nick `Source` — `Curve` `item` · optional
   - Build the shape around this curve instead. Used only when Field Info is not wired

2. `Arc Shift`  ·  nick `Shift` — `Number` `item` · default `StartlineConstants.DefaultArcShift`
   - How far each semicircle centre sits from the field centre (default 18750 mm)

3. `Arc Radius`  ·  nick `Radius` — `Number` `item` · default `StartlineConstants.DefaultArcRadius`
   - Radius of the two end semicircles (default 39000 mm)

4. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Start Line`  ·  nick `Start` — `Object` `item`
   - The start line as one object - curve plus its arc data. Wire into Axis

1. `Start Line Curve`  ·  nick `Curve` — `Curve` `item`
   - The start line itself, a closed curve in plan

2. `Arc Centers`  ·  nick `Centers` — `Point` `list`
   - The two semicircle centre points, right then left

3. `Arc Radii`  ·  nick `Radii` — `Number` `list`
   - Radius of each semicircle

4. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Startline Oval

`Antlion ▸ Bowl ▸ Section 1` · ID `STARTLINE-002` · 8 inputs · 5 outputs

> Start line as an oval around the field — four tangent arcs, or a true ellipse. Use when the bowl has no straight sides.

**Inputs**

0. `Field Info`  ·  nick `Field` — `Object` `item` · optional
   - The field, straight from Field - gives the outline and its rotation

1. `Source Outline`  ·  nick `Source` — `Curve` `item` · optional
   - Build the oval around this curve instead. Used only when Field Info is not wired

2. `Oval Mode`  ·  nick `▾ Oval` — `Text` `item` · default `StartlineConstants.DefaultOvalMode` · dropdown
   - Four tangent arcs with buildable radii, or a true ellipse (default ARC_4CENTER)

3. `Add Long` — `Number` `item` · default `StartlineConstants.DefaultAddLong`
   - How far past the field the oval reaches along its long axis (default 5000 mm)

4. `Add Short` — `Number` `item` · default `StartlineConstants.DefaultAddShort`
   - How far past the field the oval reaches along its short axis (default 5000 mm)

5. `Snap Unit`  ·  nick `Snap U` — `Number` `item` · default `StartlineConstants.DefaultSnapUnit`
   - Rounding step used by Radius Snap (default 100 mm)

6. `Radius Snap`  ·  nick `Snap` — `Boolean` `item` · default `StartlineConstants.DefaultRadiusSnap`
   - Round the arc radii to whole units so they are easier to set out on site (default false)

7. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Start Line`  ·  nick `Start` — `Object` `item`
   - The start line as one object - curve plus its arc data. Wire into Axis

1. `Start Line Curve`  ·  nick `Curve` — `Curve` `item`
   - The start line itself, a closed curve in plan

2. `Arc Centers`  ·  nick `Centers` — `Point` `list`
   - The four arc centres you set the oval out from. Empty for TRUE_ELLIPSE - a true ellipse has no arcs to centre

3. `Arc Radii`  ·  nick `Radii` — `Number` `list`
   - Radius of each arc, matching Arc Centers. Empty for TRUE_ELLIPSE, the same as Arc Centers

4. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Custom Startline

`Antlion ▸ Bowl ▸ Section 1` · ID `STARTLINE-004` · 2 inputs · 5 outputs

> Start line taken from a curve you draw, aligned to the field frame. Use when no preset shape fits. An open curve gives a stand with two free ends.

**Inputs**

0. `Field Info`  ·  nick `Field` — `Object` `item` · optional
   - The field, straight from Field. Your curve is aligned to the field frame

1. `Custom Curve`  ·  nick `Curve` — `Curve` `item` · optional
   - Your start-line curve in plan. Closed = a full ring, open = two free ends

**Outputs**

0. `Start Line`  ·  nick `Start` — `Object` `item`
   - The start line as one object - curve plus any arcs found in it. Wire into Axis

1. `Start Line Curve`  ·  nick `Curve` — `Curve` `item`
   - Your curve, aligned to the field frame

2. `Arc Centers`  ·  nick `Centers` — `Point` `list`
   - Centre point of every arc found in your curve (empty when it has none)

3. `Arc Radii`  ·  nick `Radii` — `Number` `list`
   - Radius of each detected arc, matching Arc Centers

4. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Axis

`Antlion ▸ Bowl ▸ Section 1` · ID `AXIS-002` · 9 inputs · 6 outputs

> Divides the start line into the planes each stand section is placed on. Straight runs are split by an even module, arcs by a fixed count — so section spacing follows the shape.

**Inputs**

0. `Start Line`  ·  nick `Start` — `Object` `item` · optional
   - The start line from any Startline component - the normal way to feed this

1. `Start Line Curve`  ·  nick `Curve` — `Curve` `item` · optional
   - Divide this curve instead. Used only when Start Line is not wired

2. `End Mode`  ·  nick `▾ End` — `Text` `item` · default `FILL_BOTH_ENDS` · dropdown
   - What to do with the leftover at the end of a straight run (default FILL_BOTH_ENDS)

3. `Arc Division`  ·  nick `Arc Div` — `Integer` `item` · default `8`
   - How many sections each corner arc is divided into (default 8)

4. `Module` — `Number` `item` · default `8000 mm / 300 in`
   - Section spacing on the straight runs, adjusted to fit each run evenly (default 8000 mm)

5. `Chamfer Module`  ·  nick `Chamfer` — `Number` `item` · default `0.0`
   - Spacing along chamfered corner edges. 0 = one section per chamfer (default 0)

6. `Guide Length`  ·  nick `Guide` — `Number` `item` · default `5000 mm / 200 in`
   - Length of the preview line drawn at each axis. Display only (default 5000 mm)

7. `Center Axis`  ·  nick `Center` — `Boolean` `item` · default `false`
   - Also put a section on the centre of each run (default false)

8. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Axis` — `Object` `item`
   - The axis system as one object. It also sets the axis numbers Cut refers to

1. `Base Line`  ·  nick `Base` — `Curve` `item`
   - The faceted line the stand is built on. Its outward normal sets which way it faces

2. `Axis Points`  ·  nick `Points` — `Point` `list`
   - One point per axis, sitting on the start line

3. `Axis Planes`  ·  nick `Planes` — `Plane` `list`
   - One plane per axis - where each stand section gets placed

4. `Axis Lines`  ·  nick `Lines` — `Curve` `list`
   - Preview line at each axis, for checking direction and spacing

5. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Custom Axis

`Antlion ▸ Bowl ▸ Section 1` · ID `AXIS-003` · 4 inputs · 6 outputs

> Section placement planes from axis curves you draw yourself. Each curve's crossing with the start line becomes an axis, numbered along the start line - those numbers are what Cut refers to.

**Inputs**

0. `Start Line`  ·  nick `Start` — `Object` `item` · optional
   - The start line from any Startline component - the normal way to feed this

1. `Start Line Curve`  ·  nick `Curve` — `Curve` `item` · optional
   - Cross your curves with this instead. Used only when Start Line is not wired

2. `Axis Curves`  ·  nick `Curves` — `Curve` `list` · optional
   - Your axis curves, each crossing the start line (a short curve pointing at it works too). Axes are numbered along the start line, not by list order

3. `Guide Length`  ·  nick `Guide` — `Number` `item` · default `5000 mm / 200 in`
   - Length of the preview line drawn at each axis. Display only (default 5000 mm)

**Outputs**

0. `Axis` — `Object` `item`
   - The axis system as one object. It also sets the axis numbers Cut refers to

1. `Base Line`  ·  nick `Base` — `Curve` `item`
   - The faceted line the stand is built on. Its outward normal sets which way it faces

2. `Axis Points`  ·  nick `Points` — `Point` `list`
   - One point per axis, numbered along the start line

3. `Axis Planes`  ·  nick `Planes` — `Plane` `list`
   - One plane per axis - facing follows your curve, away from the field

4. `Axis Lines`  ·  nick `Lines` — `Curve` `list`
   - Preview line at each axis, for checking direction and spacing

5. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

◀ [README](index.md) · next ▶ [Section 2 · Section](02-section.md)
