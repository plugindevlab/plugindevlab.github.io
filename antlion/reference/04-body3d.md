---
layout: default
title: "Section 4 · 3D Solids"
generated: true
---

# Section 4 · 3D Solids

`Antlion ▸ Bowl ▸ Section 4` — build what 2D decided as real solids.

> Generated from the plugin source by `refdoc_check.py --sync` — the text below is
> the plugin's own tooltip text. Do not edit this file; it is rewritten on every sync.
> Layout, standard chains and shared ports: [README](index.md).

---

## Stand 3D

`Antlion ▸ Bowl ▸ Section 4` · ID `STAND-001` · 7 inputs · 5 outputs

> Sweeps the closed section profile around the axis planes into the solid tier bodies. This is the raw stand — cuts and openings are applied later by Stand 3D Plus.

**Inputs**

0. `Handrail Sections`  ·  nick `Rails` — `Curve` `list` · optional
   - Barrier section curves from Section Plus. Wire them to build barriers as solids

1. `Stand Sections`  ·  nick `SSD` — `Object` `list` · optional
   - The tier sections to sweep. Wire from Stand 2D, or from Section Plus

2. `Axis` — `Object` `item` · optional
   - The axis system, passed along the chain. Wire from Stand 2D

3. `Axis Join Mode`  ·  nick `▾ Join` — `Text` `item` · default `AUTO` · dropdown
   - How sections are joined between axes: AUTO, RADIAL or ORTHO (default AUTO)

4. `Generate Brep`  ·  nick `Brep` — `Boolean` `item` · default `true`
   - Build the stand solids. Off = fast wireframe-only check (default true)

5. `Generate Handrail`  ·  nick `Handrail` — `Boolean` `item` · default `false`
   - Build the barriers as solids too. Needs Handrail Sections wired (default false)

6. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Stand Body`  ·  nick `SBD` — `Object` `item`
   - The raw tier solids as one object. Cuts and openings are subtracted from this

1. `Stand Geometry`  ·  nick `Bodies` — `Brep` `list`
   - The tier bodies as separate solids, one per tier

2. `Handrail Geometry`  ·  nick `Rails 3D` — `Brep` `list`
   - The barriers as solids, one per tier. Empty unless Generate Handrail is on

3. `Section Profiles`  ·  nick `Profile` — `Curve` `list`
   - The closed profile as actually placed on each axis plane - check here when a sweep looks wrong

4. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Vom 3D

`Antlion ▸ Bowl ▸ Section 4` · ID `VOM3D-001` · 8 inputs · 4 outputs

> Builds the vomitories as solids — the side and end walls, the portal opening punched through, and the stair or ramp down to the concourse.

**Inputs**

0. `Seat Result`  ·  nick `SRD` — `Object` `item` · optional
   - The 2D layout, from Seat or a Cut. It carries the footprints and tread ranges

1. `Wall Thickness`  ·  nick `Wall T` — `Number` `item` · default `150 mm / 6 in`
   - Thickness of the vomitory walls, measured inward (default 150 mm)

2. `Wall Rise`  ·  nick `Wall R` — `Number` `item` · default `600 mm / 24 in`
   - How far the end wall rises above the tread behind it (default 600 mm)

3. `Opening Widths`  ·  nick `Open W` — `Number` `list` · optional
   - Clear width of the portal through the end wall - one per tier (default 2700 mm)

4. `Opening Heights`  ·  nick `Open H` — `Number` `list` · optional
   - Clear height of the portal from the vomitory floor - one per tier (default 2400 mm)

5. `Stair Width`  ·  nick `Stair W` — `Number` `item` · default `1200 mm / 48 in`
   - Width of each stair strip, inside the side wall. Type 2 only (default 1200 mm)

6. `Stair Side Mode`  ·  nick `▾ Stair` — `Text` `list` · optional · dropdown
   - Which side the stairs run down: NONE, LEFT, RIGHT or BOTH - one per tier. Type 2 only (default BOTH)

7. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Seat Result`  ·  nick `SRD` — `Object` `item`
   - The layout back again, cut range widened for stepped vomitories. Wire THIS on, not the original

1. `Vom Breps`  ·  nick `Vom 3D` — `Brep` `tree`
   - One branch per tier. Walls, portal surround, and the stair or ramp

2. `Vom Curves`  ·  nick `Vom Crv` — `Curve` `tree`
   - One branch per tier. The lines the walls were built along

3. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Stand 3D Plus

`Antlion ▸ Bowl ▸ Section 4` · ID `BUILD-001` · 5 inputs · 5 outputs

> Builds the 3D bowl: cuts the stand solids with everything the 2D chain decided — shape cuts, entrance openings, vomitories — and lifts the seats and aisle steps to their real tread heights.

**Inputs**

0. `Stand Body`  ·  nick `SBD` — `Object` `item` · optional
   - The raw stand solids from Stand 3D. Unwired = rebuilt from the rings, rougher

1. `Seat Result`  ·  nick `SRD` — `Object` `item` · optional
   - The 2D layout to build. Wire from Seat, or from Cut once you have cut the bowl

2. `WC Deck Mode`  ·  nick `▾ Deck` — `Integer` `item` · default `0` · dropdown
   - 0 = INTEGRAL, cast into the tier body. 1 = PLATFORM, a separate deck on top (default 0)

3. `Tier Enabled`  ·  nick `Tiers` — `Boolean` `list` · optional
   - Which tiers to build - one per tier, empty = every tier (default true)

4. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Build Result`  ·  nick `BRD` — `Object` `item`
   - The 3D bowl as one object. Wire into Rail 3D

1. `Stand Breps`  ·  nick `Bodies` — `Brep` `tree`
   - One branch per tier. A tier with vomitories is left open at the void - not an error

2. `Aisles 3D` — `Brep` `tree`
   - One branch per tier. Aisle steps as solids, raised to read as a real staircase

3. `WC Decks` — `Brep` `tree`
   - One branch per tier. Only filled in PLATFORM mode or when an INTEGRAL merge failed

4. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Rail 3D

`Antlion ▸ Bowl ▸ Section 4` · ID `RAIL-001` · 11 inputs · 8 outputs

> Builds the railings and parapets as solids — the front barrier, the rear parapet, the edges left by a cut, and the walls around entrance openings.

**Inputs**

0. `Build Result`  ·  nick `BRD` — `Object` `item` · optional
   - The 3D bowl from Stand 3D Plus. This one wire is the only input needed

1. `Side Top Mode`  ·  nick `▾ SideTop` — `Text` `list` · default `Hold Side H` · dropdown
   - How tall the cut-edge parapet is along its run - one per tier. Hold Side H keeps Side H the whole way, so it meets the rear parapet at a step. Blend to Rear H starts at Side H and eases to Rear H by the back, so the two meet flush (default Hold Side H)

2. `Front Skirt Depths`  ·  nick `Front Sk` — `Number` `list` · default `-1.0`
   - How far the front parapet drops below the first tread - one per tier. Leave it at -1 to use the barrier depth from Section Plus. 0 = no skirt

3. `Side Heights`  ·  nick `Side H` — `Number` `list` · default `1200 mm / 48 in`
   - Height of the parapet along a cut edge, measured above each tread - one per tier. 0 leaves the side railings out (default 1200 mm)

4. `Side Skirt Depths`  ·  nick `Side Sk` — `Number` `list` · default `-1.0`
   - How far the cut-edge parapet drops below the tread at each end of its run - in between it hangs lower still, covering the cut face and the structure below - one per tier. Leave it at -1 to use the barrier depth from Section Plus. 0 = no skirt

5. `Rear Heights`  ·  nick `Rear H` — `Number` `list` · default `1200 mm / 48 in`
   - Height of the rear parapet above the last tread - one per tier. 0 leaves it out, the same as switching it off (default 1200 mm)

6. `Rear Skirt Depths`  ·  nick `Rear Sk` — `Number` `list` · default `-1.0`
   - How far the rear parapet drops below the last tread - one per tier. Leave it at -1 to use the barrier depth from Section Plus. 0 = no skirt

7. `Opening Rail Heights`  ·  nick `Open H` — `Number` `list` · default `-1.0`
   - Height of the parapet around an entrance opening, above the tread - one per tier. Leave it at -1 to follow the front parapet height from Section Plus. Give a height to use that instead - it still meets the front parapet at its height where the two join. 0 leaves it out

8. `Side Base Runs`  ·  nick `Side Run` — `Number` `list` · default `-1.0`
   - How far the underside of the cut-edge parapet runs level from the front before it starts to climb, hiding the structure below - one per tier. -1 = the depth of the first row, 0 = climbs right away

9. `Rear Enabled`  ·  nick `Rear On` — `Boolean` `list` · default `true`
   - Build the rear parapet at the back of each tier (default true)

10. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Rail Result`  ·  nick `RLD` — `Object` `item`
   - All the railings as one object - solids, their kinds and tiers

1. `Railing Breps`  ·  nick `Rails` — `Brep` `tree`
   - One branch per tier. Every railing as a solid, all kinds together

2. `Front Rails`  ·  nick `Front` — `Brep` `tree`
   - One branch per tier. The front parapet at the leading edge of each tier

3. `Side Rails`  ·  nick `Side` — `Brep` `tree`
   - One branch per tier. The parapet along each cut edge, rising with the treads

4. `Rear Rails`  ·  nick `Rear` — `Brep` `tree`
   - One branch per tier. The parapet at the back of each tier

5. `Opening Rails`  ·  nick `Open` — `Brep` `tree`
   - One branch per tier. The parapet around each entrance opening, climbing along its sides and level across the back

6. `Rail Curves`  ·  nick `Rail Crv` — `Curve` `tree`
   - One branch per tier. The lines each railing was built along

7. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

## Assemble Bowl

`Antlion ▸ Bowl ▸ Section 4` · ID `UTIL-004` · 7 inputs · 7 outputs

> Assembles the final bowl plan before Stand 3D Plus: merges parallel seat layouts (tiers renumbered), pulls exposed cut ends past their axis by the offset, and previews the finished plan.

**Inputs**

0. `Stand Bodies`  ·  nick `SBD` — `Object` `list` · optional
   - The stand solids for each branch, in the same order as the layouts below

1. `Seat Results`  ·  nick `SRD` — `Object` `list` · optional
   - One layout per branch. Tiers are renumbered in the order you supply them

2. `Cut End Mode`  ·  nick `▾ End` — `Text` `item` · default `EXTEND` · dropdown
   - How the pulled end runs on: EXTEND keeps the remaining tread's straight line (default), FOLLOW keeps the ring's own path

3. `Opening End Mode`  ·  nick `▾ Op End` — `Text` `item` · default `EXTEND` · dropdown
   - Opening edges only: EXTEND (default) or FOLLOW, set separately from the cut ends

4. `Cut Offset`  ·  nick `Offset` — `Number` `item` · default `0.0`
   - Pull every exposed cut end this far past its axis, so the joint sits clear of the structural line. Ends where two layouts meet at the same level stay on the axis. 0 = cut exactly on the axis (default)

5. `Opening Offset`  ·  nick `Op Offset` — `Number` `item` · default `-1.0`
   - Opening edges only (Opening Cut spans): their own pull distance. -1 = follow Cut Offset (default), 0 = leave openings on the axis

6. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Stand Body`  ·  nick `SBD` — `Object` `item`
   - The merged stand solids, same tier order. Empty when nothing was wired above

1. `Seat Result`  ·  nick `SRD` — `Object` `item`
   - The assembled layout, carrying every branch's tiers with cut ends resolved. Wire into Stand 3D Plus

2. `Tread Rings`  ·  nick `Rings` — `Curve` `tree`
   - One branch per tier. The tread rings of the final plan, cut ends pulled to their offset

3. `Seat Rects`  ·  nick `Seats` — `Curve` `tree`
   - One branch per tier. Every seat in the final plan

4. `Vom Rects`  ·  nick `Voms` — `Curve` `tree`
   - One branch per tier. The vomitories in the final plan

5. `Aisles` — `Curve` `tree`
   - One branch per tier. The aisle cells in the final plan

6. `Debug` — `Text` `item`
   - One-line diagnostic including the tier map. Copy this when reporting a problem

---

## Seat 3D

`Antlion ▸ Bowl ▸ Section 4` · ID `SEAT3D-001` · 13 inputs · 3 outputs

> Puts a chair and a seated spectator target on every seat of the built bowl. Wire the Stand 3D Plus BRD output. The ports carry one merged preview mesh per tier so twenty thousand seats stay light; bake the component itself (right-click, Bake) to plant real block instances - one definition, one instance per seat - so the document stays light too. The spectator is a flat target whose eye mark sits exactly on the eye point the C-value analysis measures from.

**Inputs**

0. `Build Result`  ·  nick `BRD` — `Object` `item`
   - The built bowl, straight from Stand 3D Plus. Carries the 3D seat planes and tiers

1. `Custom Chair Parts`  ·  nick `Cust Chr` — `Geometry` `list` · optional
   - Your own chair geometry around the origin: X = width, Y = to the pitch, Z = up

2. `Custom Human Parts`  ·  nick `Cust Hum` — `Geometry` `list` · optional
   - Your own spectator geometry, modelled around the origin like Custom Chair Parts

3. `Seat Map Data`  ·  nick `SMD` — `Object` `item` · optional
   - The seat map from Seat Map. Baked blocks then carry their seat address as user text

4. `Side Gap` — `Number` `item` · default `60 mm / 2.5 in`
   - Gap to the neighbouring chair, taken off the seat width. 0 fills it (default 60 mm)

5. `Depth Gap`  ·  nick `Depth Gp` — `Number` `item` · default `60 mm / 2.5 in`
   - Knee room in front, taken off the seat depth. 0 fills it (default 60 mm)

6. `Chair` — `Boolean` `item` · default `true`
   - Build a chair on every seat (default true)

7. `Human` — `Boolean` `item` · default `false`
   - Stand a seated spectator on every seat, eye mark on the analysis eye point (default false)

8. `Tier Enabled`  ·  nick `Tiers` — `Boolean` `list` · optional
   - Which tiers to build - one per tier, empty = every tier (default true)

9. `Merge Preview`  ·  nick `Merge` — `Boolean` `item` · default `false`
   - Deprecated - no effect. Kept so older files keep loading

10. `Block Prefix`  ·  nick `Prefix` — `Text` `item` · default `Antlion`
   - Name prefix for the baked block definitions (default Antlion)

11. `Bake Blocks`  ·  nick `Bake` — `Boolean` `item` · default `false`
   - Wire a Button to plant blocks in the Rhino document - one definition per kind, one instance per seat

12. `Panel Values`  ·  nick `Panel` — `Object` `item` · optional
   - Every value from Input Panel, as one bundle. Anything you wire in yourself wins over it

**Outputs**

0. `Chairs` — `Geometry` `list`
   - One chair solid per seat. Built only while something is wired here

1. `Humans` — `Geometry` `list`
   - One spectator target per seat. Built only while something is wired here

2. `Debug` — `Text` `item`
   - One-line diagnostic. Copy this text when reporting a problem

---

◀ [Section 3 · 2D Plan](03-plan2d.md) · next ▶ [Analysis & Util](05-analysis-util.md)
